# 项目修改记录文档

| 时间 | 修改摘要 | 状态 |
| --- | --- | --- |
| 2026-09-08 | 修复 Issue1：`export_layer_crop` 缺少 `Image.open` 异常处理，当 `composite.png` 不存在时改为抛出友好 `ValueError`（"文档 {doc_id} 未生成合成图，请重新解析"），已验证通过 | 修复完成 |
| 2026-09-08 | 修复矢量图层解析失败：补充 `aggdraw` 依赖（`psd-tools[composite]`）并重启服务 | 修复完成 |
| 2026-09-08 | 新增 `requirements.txt` 统一依赖管理，并同步 README 安装说明 | 已完成 |
| 2026-09-08 | 新增一键启动脚本 `start.sh`（自动创建虚拟环境、按需安装依赖、启动 Web 服务），已实测启动成功，同步 README 快速开始 | 已完成 |
| 2026-09-08 | `start.sh` 增加自动停止：启动前自动清理占用 8642 端口的旧服务进程（先 TERM 后 KILL），可重复执行不再报端口占用，已实测验证 | 已完成 |
| 2026-09-08 | 重构 Issue（重复计算裁剪边界）：`index.html` 提取公共函数 `clampBbox`，统一 `collectSpots` 与 `pickSpot` 的 bbox 画布钳制逻辑，JS 语法检查与 6 组边界用例单测全部通过 | 已完成 |
| 2026-09-08 | 功能优化：自由测量方向跟随鼠标——x 按鼠标在图层左/右半侧从左/右边缘起测，y 按鼠标在上/下半侧从上/下边缘起测，Δx/Δy 恒为正值，与吸附测量方向逻辑一致；mock 环境四象限+分界点单测全部通过 | 已完成 |
| 2026-09-08 | 修复页面缓存问题：`app.py` 首页响应增加 `Cache-Control: no-store`，浏览器不再缓存 index.html，脚本更新即时生效（已验证响应头生效） | 修复完成 |
| 2026-09-08 | 功能优化：吸附测量同样方向跟随鼠标——`axisMeasure` 重叠时按鼠标在重叠区中点左/右选左-左/右-右对齐边（上下同理），Δx/Δy 显示取绝对值恒正；单测含截图复现场景全部通过 | 已完成 |
| 2026-09-08 | 新增「导出静态网页」功能（规划见 .trae/specs/export-static-html/）：`core.py` 新增 `export_html_zip`（图层树遍历/命名防冲突/HTML+CSS 生成/文本真实渲染/多画板多页面/zip 打包，解析期写入画板信息），`app.py` 新增 `GET /api/export/<doc_id>/html?layout=vw|px`，前端新增「导出网页」按钮 + 布局模式下拉；单测与浏览器实测通过 | 已完成 |
| 2026-09-08 | 修复网页导出切图不纯净：叶子切图由合成图 bbox 裁剪（会混入区域内其他元素）改为 `render_layer_png` 图层自身像素渲染，并裁掉透明边输出最小切图（HTML 定位/宽高同步偏移收缩）；`_locate_layer` 增加 PSDImage 缓存避免批量切图重复解析 PSD；实测切图纯净、zip 体积 5.58MB→4.32MB | 修复完成 |
| 2026-09-08 | 修复导出页面布局失效：`_node_css_rule` 生成的 CSS 规则结尾多一个 `}`，导致浏览器丢弃后续全部定位规则（图片按文档流堆叠）；修复后浏览器 DOM 实测 10/10 切图渲染位置/尺寸与图层 bbox 百分比完全吻合，导出页面与工作区视觉一致 | 修复完成 |
| 2026-09-08 | 调整网页导出 zip 下载文件名为设计稿名称（去掉 `-html` 后缀），已验证 Content-Disposition 输出 | 已完成 |
| 2026-09-08 | 修复导出下载名显示 `zip.zip`：前端 Content-Disposition 解析正则误匹配 `filename=.zip`，改为优先解析 `filename*=`（RFC 5987 中文）且回退设计稿名；node 单测两种响应头形态均通过 | 修复完成 |
| 2026-09-08 | 修复 Issue（PSD 缓存无上限可能内存泄漏）：`_PSD_CACHE` 由普通 dict 改为 OrderedDict LRU（上限 4 条，命中刷新顺序、超限淘汰最久未使用），mock 单测容量/淘汰/命中与真实渲染回归全部通过 | 修复完成 |
